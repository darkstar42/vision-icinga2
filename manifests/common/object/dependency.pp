# documentation
class vision_icinga2::common::object::dependency {

  # ::icinga2::object::apply_dependency { "parent-${::fqdn}":
  #   parent_host_name      => $::fqdn,
  #   assign_where          => "host.address && host.vars.parent && host.vars.parent == \"${::fqdn}\"",
  #   disable_notifications => true,
  #   disable_checks        => true
  # }

}
